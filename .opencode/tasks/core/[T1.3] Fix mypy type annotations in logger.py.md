---
status: DONE
completed: 2026-05-03
---

[T1.3] Fix mypy type annotations in logger.py
Priority: P5
Findings: F-TYPE-01, F-TYPE-02, F-TYPE-03, F-TYPE-04, F-TYPE-05, F-TYPE-06
Depends: —

Phase A — Plan mode (НЕ ПИСАТЬ КОД)
1. Прочитать все Inputs
2. Построить карту затронутых файлов и слоёв
3. Проверить: не нарушает ли план core→engine→bots→ui
4. Проверить: spawn-совместимость новых компонентов
5. ExitPlanMode → ждать "approved" от пользователя
6. Только после approve переходить к Phase B

Phase B — Implementation

Context:
   logger.py содержит 6 mypy ошибок: отсутствующие type annotations
   и untyped Queue generic. Это не ломает runtime, но нарушает strict
   typing контракт проекта.
   Слой: core
   Spawn-safe: не применимо

Inputs:
   - core/logger.py                 (все строки с mypy errors)

Lint/Type gate (до написания кода):
   Текущее состояние:
   - mypy: 6 errors в logger.py
   - ruff: 0 реальных ошибок
   Цель: mypy → 0

Steps:
   - [ ] logger.py:30 — ANN204: добавить -> None в AsyncDiscordHandler.__init__
   - [ ] logger.py:34 — type-arg: Queue[logging.LogRecord] или Queue[dict[str, Any]]
   - [ ] logger.py:80 — attr-defined: "object" has no attribute "append"
         → _log_listener.handlers — нужно добавить type narrowing или cast
   - [ ] logger.py:96 — no-untyped-def: добавить type annotations:
         def _handle_exceptions(
             exc_type: type[BaseException],
             exc_value: BaseException | None,
             exc_traceback: types.TracebackType | None,
         ) -> None
   - [ ] logger.py:111 — type-arg: Queue[logging.LogRecord]
   - [ ] logger.py:120 — var-annotated: добавить аннотацию типа для log_queue
   - [ ] logger.py:166 — type-arg: Queue[logging.LogRecord]
   - [ ] mypy core/logger.py --strict --ignore-missing-imports
   - [ ] ruff check core/logger.py

Acceptance:
   - ✓ mypy --strict: 0 ошибок в logger.py
   - ✓ ruff: 0 ошибок в logger.py
   - ✓ API не изменён

Report:
   - Было/стало: mypy 6 → 0

Time: 0.5ч
Branch: task/T1.3-fix-mypy-logger
PR: [T1.3] fix: add missing type annotations to core/logger.py
Depends: —
