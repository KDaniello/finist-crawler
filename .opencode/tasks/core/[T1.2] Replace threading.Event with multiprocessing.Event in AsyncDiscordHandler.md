---
status: DONE
completed: 2026-05-02
---

[T1.2] Replace threading.Event with multiprocessing.Event in AsyncDiscordHandler
Priority: P2
Findings: F-SPAWN-02
Depends: —

Phase A — Plan mode (НЕ ПИСАТЬ КОД)
1. Прочитать все Inputs
2. Построить карту затронутых файлов и слоёв
3. Проверить: не нарушает ли план core→engine→bots→ui
4. Проверить: spawn-совместимость новых компонентов
5. ExitPlanMode → ждать "approved" от пользователя
6. Только после approve переходить к Phase B

Phase B — Implementation

Context:
   AsyncDiscordHandler в logger.py использует threading.Event и
   threading.Thread. Этот handler живёт только в главном процессе
   (QueueListener), не в воркерах. Thread + Event корректны для
   главного процесса. Но архитектурно это вводит в заблуждение:
   core/ должен быть spawn-safe, и любой threading.* в нём —
   красный флаг при аудите.

   ОЦЕНКА: После анализа — threading.Thread и threading.Event здесь
   оправданы (handler работает в главном процессе, не передаётся
   через pickle). Это FALSE POSITIVE для SPAWN-аудита.

   РЕШЕНИЕ: Не менять. Вместо этого добавить явный комментарий,
   объясняющий почему threading.* здесь безопасен.

   Слой: core
   Spawn-safe: да (только главный процесс)

Inputs:
   - core/logger.py                 (AsyncDiscordHandler class)

Lint/Type gate (до написания кода):
   Текущее состояние:
   - ruff: 0 реальных ошибок в logger.py
   - mypy: 6 ошибок в logger.py (type annotations)
   Цель: mypy → 0, ruff → 0

Steps:
   - [ ] Добавить комментарий над AsyncDiscordHandler:
         "# SPAWN-SAFE: handler exists only in main process via QueueListener.
         #  threading.Thread/Event are correct here — never pickled."
   - [ ] ruff check core/logger.py

Acceptance:
   - ✓ Явный комментарий объясняет threading.* использование
   - ✓ Код не изменён (threading.* здесь корректен)

Report:
   - Изменение: только добавлен комментарий-документация

Time: 0.1ч
Branch: task/T1.2-document-threading-safety
PR: [T1.2] docs: add spawn-safety comment to AsyncDiscordHandler
Depends: —
